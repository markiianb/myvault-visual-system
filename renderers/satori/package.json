{
  "name": "myvault-satori-renderer",
  "version": "0.1.0",
  "description": "Satori renderer for MyVault social-image asset chunk. Produces PNG images from JSX components that inherit brand tokens. Pinned to satori (subset-of-CSS Wasm layout) + @resvg/resvg-js (SVG -> PNG). No Chromium.",
  "private": true,
  "type": "module",
  "scripts": {
    "build:tokens": "node scripts/build-tokens.mjs",
    "render": "tsx scripts/render.ts",
    "render:all": "tsx scripts/render-all.ts"
  },
  "dependencies": {
    "@resvg/resvg-js": "^2.6.2",
    "react": "^18.3.1",
    "satori": "^0.12.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/react": "^18.3.12",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
